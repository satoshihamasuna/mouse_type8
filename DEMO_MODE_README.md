# demo モード操作ガイド

`System/Src/demo.cpp` と `System/Src/demo2.cpp` に実装されている demo モードの操作方法と、各項目の動作をまとめます。

> [!WARNING]
> 走行を伴う項目があります。機体を迷路のスタート位置 `(0, 0)` に北向きで置き、走行可能な状態で操作してください。

## 前提設定

探索と最短走行の目的地は `Params/mouse_config.h` で設定されています。現在の設定は次のとおりです。

| 設定 | 値 |
| --- | --- |
| スタート | `(0, 0)`、北向き |
| ゴール座標 | `(7, 7)` |
| ゴールサイズ | `2` |
| demo 1 の初期探索制限時間 | 6 分 |

## demo モードに入る

1. 電源投入またはリセット後、メインモード選択画面で右ホイールを回します。
2. LED 表示を `0x10`（モード番号 `1`）に合わせます。
3. 左ホイールを回して確定状態にします。
4. `Demo()`（以下「demo 1」）の項目選択画面に入ります。

メインモード選択では、右ホイールがモード選択、左ホイールが確定操作です。モード番号は `0`～`3` で循環します。

## 共通の操作方法

demo 1 と demo 2 は同じ方法で操作します。

| 操作 | 内容 |
| --- | --- |
| 右ホイールを回す | 項目を `0x0`～`0xF` から選択する |
| 左ホイールを回す | 選択項目を確定する。確定中は LED が約 400 ms 間隔で点滅する |
| 前方を手や壁で覆う | 前方 IR センサのしきい値を超えると、確定した項目を実行する |
| 項目 `0xF` を実行 | 現在の demo 画面を終了する |

右ホイールによる項目変更は、左ホイールで確定する前だけ有効です。誤って確定した場合は、左ホイールを反対方向へ回して未確定状態へ戻してから選び直してください。

実行開始時には LED が約 50 ms 間隔で点滅します。走行中は機体に触れないでください。

## 画面の遷移

demo モードでは、demo 1 と demo 2 が連続して実行されます。

```text
メインモード選択
  └─ モード 1 を確定
       └─ demo 1
            └─ 項目 0xF で終了
                 └─ demo 2
                      └─ 項目 0xF で終了
                           └─ メインモード選択へ戻る
```

demo 1 の `0xF` だけではメインモード選択へ戻らない点に注意してください。

## demo 1 の項目

demo 1 は、探索、その場で保持している壁情報を使う経路確認、および最短走行を行う画面です。

| 項目 | 動作 |
| --- | --- |
| `0x0` | 加速付き第1優先ゴール探索で往路を走行し、復路は枝刈り付き全面探索でスタートへ戻る |
| `0x1` | 第1優先ゴール探索で往路・復路を走行する |
| `0x2` | 加速付き第2優先ゴール探索で往路を走行し、復路は加速付き全面探索でスタートへ戻る |
| `0x3` | 加速付き第2優先ゴール探索で往路・復路を走行する |
| `0x4` | 1000 mm/s のターンテーブルで Dijkstra 経路の有無を確認する。成功時は LED が流れる |
| `0x5` | 700 mm/s 系パラメータで Dijkstra 最短走行を行う |
| `0x6` | 探索制限時間の選択画面を開く |
| `0x7` | 壁履歴の確認画面を開く |
| `0x8` | 1600 mm/s 系、吸引 `700`、加速ターン `v1` で最短走行する |
| `0x9` | 1400 mm/s 系、吸引 `600`、パラメータ `v1` で最短走行する |
| `0xA` | 1400 mm/s 系、吸引 `650`、直進 `v2`／斜め `v1` で最短走行する |
| `0xB` | 1600 mm/s 系、吸引 `650`、パラメータ `v1` で最短走行する |
| `0xC` | 1600 mm/s 系、吸引 `700`、直進 `v2`／斜め `v1` で最短走行する |
| `0xD` | 1600 mm/s 系、吸引 `700`、加速ターン `v2` で最短走行する |
| `0xE` | 1600 mm/s 系、吸引 `700`、加速ターン `v3` で最短走行する |
| `0xF` | demo 1 を終了し、demo 2 へ進む |

### 探索データ

demo 1 に入ると迷路と壁履歴を初期化し、作業 RAM へ書き込みます。項目 `0x0`～`0x3` の探索が正常終了すると、壁情報と壁履歴が保存されます。同じ demo 1 内の最短走行項目は、その時点で `wall_data` が保持している壁情報を使用します。

探索中にエラーが発生した場合は、今回追加された壁履歴を取り消して保存し直します。

## demo 2 の項目

demo 2 はマップ生成の確認と、別セットの最短走行パラメータを試す画面です。demo 2 開始時には新しい `wall_data` が生成され、迷路情報は初期化されます。

| 項目 | 動作 |
| --- | --- |
| `0x0` | ゴール向け歩数マップと、閉壁条件を反映した歩数マップを生成してシリアル出力する |
| `0x1` | 未実装（何もしない） |
| `0x2` | 未実装（何もしない） |
| `0x3` | 確認点滅のみ |
| `0x4` | 1000 mm/s のターンテーブルで Dijkstra 経路の有無を確認する。成功時は LED が流れる |
| `0x5` | Dijkstra 経路確認走行を行う |
| `0x6` | 500 mm/s 系パラメータで Dijkstra 最短走行を行う |
| `0x7` | 壁履歴の確認画面を開く |
| `0x8` | 1800 mm/s 系、吸引 `700`、パラメータ `v1` で最短走行する |
| `0x9` | 2000 mm/s 系、吸引 `800`、パラメータ `v1` で最短走行する |
| `0xA` | 1800 mm/s 系パラメータ、吸引 `800`、加速ターン `v1` で最短走行する |
| `0xB` | 1200 mm/s 系、吸引 `600`、パラメータ `v0` で最短走行する |
| `0xC` | 1200 mm/s 系、吸引 `600`、パラメータ `v1` で最短走行する |
| `0xD` | 1400 mm/s 系、吸引 `650`、パラメータ `v0` で最短走行する |
| `0xE` | 1400 mm/s 系、吸引 `650`、パラメータ `v1` で最短走行する |
| `0xF` | demo 2 を終了し、メインモード選択へ戻る |

## エラー表示

探索または最短走行でエラーが検出されると、全 LED が約 80 ms 間隔で点滅し続けます。機体を安全な状態にしてから本体ボタンを押すと、エラー表示を終了できます。

## 実装上の注意

- demo 1 の前方 IR 実行しきい値は `IrSensor_Avg() > 2000` です。
- demo 2 は項目によって `IrSensor_Avg() > 2000` または `> 8000` を使用しています。反応しない場合は前方センサをより近くで確実に覆ってください。
- demo 2 の `0xA` はターン時間に `mode_2000`、走行パラメータに 1800 mm/s 系を指定しています。本書では実際に渡される走行パラメータを基準に記載しています。
- demo 2 の `0xC` はターン時間に `mode_1400`、走行パラメータに 1200 mm/s 系を指定しています。本書では実際に渡される走行パラメータを基準に記載しています。
- LED の物理的な並びとビット順は一致していません。対応は `Peripheral/Src/interface.c` の `Indicate_LED()` を参照してください。

## 関連ファイル

| ファイル | 内容 |
| --- | --- |
| `System/Src/mode.cpp` | メインモード選択と demo 1／demo 2 の呼び出し |
| `System/Src/demo.cpp` | demo 1 |
| `System/Src/demo2.cpp` | demo 2 |
| `System/Src/demo_util.cpp` | 探索方式の振り分け |
| `System/Src/search_time_select.cpp` | 探索制限時間の選択 |
| `System/Src/wall_histry_check.cpp` | 壁履歴の確認 |
| `Params/mouse_config.h` | ゴール座標とゴールサイズ |
